# values.yaml
replicaCount: 1
image:
  repository: <your-docker-image>
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
ingress:
  host: <fqdn-de-lingress>
colorSecretName: "color-secret"
color: "color-pas-secret"
appConfigName: "app-config"
